import styled from 'styled-components';

export const ToggleButton = styled.button`
  padding: 0 16px;
  cursor: pointer;
  background: none;
  border: none;
  font-weight: bold;
  transition: all 0.3s ease;
  margin-left: auto;
`;